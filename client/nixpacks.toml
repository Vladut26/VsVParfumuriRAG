[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["rm -rf node_modules package-lock.json", "npm install --include=optional"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx serve dist -s -l $PORT"